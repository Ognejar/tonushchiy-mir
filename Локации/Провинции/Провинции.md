---
project: Тонущий_мир
type: "Индекс"
---
# Провинции проекта
```dataview
LIST FROM ""
WHERE tags AND contains(tags, "провинция")
AND project = this.project
AND file.name != this.file.name
SORT file.name ASC
```